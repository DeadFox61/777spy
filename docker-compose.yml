version: "3"

services:
  postgresdb:
    build:
      context: ./postgres
      dockerfile: Dockerfile
    environment:
      - POSTGRES_PASSWORD=As3Vdsd898
    volumes:
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./postgres/pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
  web:
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - ./:/usr/src/app
    depends_on:
      - postgresdb
    ports:
      - "8000:8000"
  parse_ezugi_roulette:
    build:
      context: ./parsing
      dockerfile: Dockerfile_ezugi_roulette
    volumes:
      - ./parsing:/usr/src/app
      - ./logs:/usr/src/app/logs
    depends_on:
      - postgresdb
  parse_ezugi_baccarat:
    build:
      context: ./parsing
      dockerfile: Dockerfile_ezugi_baccarat
    volumes:
      - ./parsing:/usr/src/app
      - ./logs:/usr/src/app/logs
    depends_on:
      - postgresdb
  parse_evo_roulette:
    build:
      context: ./parsing
      dockerfile: Dockerfile_evo_roulette
    volumes:
      - ./parsing:/usr/src/app
      - ./logs:/usr/src/app/logs
    depends_on:
      - postgresdb
  parse_evo_baccarat:
    build:
      context: ./parsing
      dockerfile: Dockerfile_evo_baccarat
    volumes:
      - ./parsing:/usr/src/app
      - ./logs:/usr/src/app/logs
    depends_on:
      - postgresdb
      
